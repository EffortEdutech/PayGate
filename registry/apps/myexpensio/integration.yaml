schema_version: "1.0"
authentication: jwt
features:
  checkout: true
  subscriptions: true
  customer_portal: true
  entitlements: true
return_contexts:
  billing:
    success_path: /billing?billing=success
    cancel_path: /billing?billing=cancelled
    portal_path: /billing?billing=portal_return
